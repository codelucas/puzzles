/*
 * You have a set of envelopes of different widths and heights. One envelope
 * can fit into another if and only if both the width and height of one
 * envelope is greater than the width and height of the other envelope. What
 * is the maximum number of envelopes can you russian doll?
 */
 
int main(int argc, char *argv[])
{
    return 0;
}
